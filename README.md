# MCO Dashboard (starter)

Scaffolded React + TypeScript + Vite + Tailwind project.

Scripts:

- `npm run dev` — start dev server
- `npm run build` — build production
- `npm run preview` — preview production build
